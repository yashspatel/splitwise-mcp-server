[project]
name = "splitwise-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.14.4",
    "python-dotenv>=1.2.1",
    "splitwise>=3.0.0",
]
