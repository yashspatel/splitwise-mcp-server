[project]
name = "splitwise-mcp-local"
version = "0.1.0"
description = "Local Splitwise MCP server (FastMCP) with preview-confirm safety."
requires-python = ">=3.11"
dependencies = [
  "fastmcp>=2.2.11",
  "splitwise>=3.0.0",
  "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
